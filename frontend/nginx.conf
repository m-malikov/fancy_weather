worker_processes 1;

events {

}

http {
    include mime.types;
    root /data/www;
    index index.html;
    server {
        listen 80;

        location /api/  {
            # put app hostname there
            default_type application/json;
            return 200 '{"text": "Sample text\\nSample text", "image": "http://vasnec.ru/img/4/vasnecov_a84.jpg"}';
        }
    }
}
